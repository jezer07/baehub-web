<%= form_with model: event, class: "space-y-5", data: { controller: "event-form" } do |f| %>
  <% if event.errors.any? %>
    <div class="rounded-xl bg-red-50 border border-red-200 p-4">
      <h3 class="text-sm font-semibold text-red-800 mb-2">
        <%= pluralize(event.errors.count, "error") %> prevented this event from being saved:
      </h3>
      <ul class="list-disc list-inside text-sm text-red-700 space-y-1">
        <% event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <label class="block text-sm font-medium text-neutral-700 mb-2">
      Event title
    </label>
    <%= f.text_field :title, autofocus: true, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors", placeholder: "Enter event title" %>
  </div>

  <div>
    <label class="block text-sm font-medium text-neutral-700 mb-2">
      Description (optional)
    </label>
    <%= f.text_area :description, rows: 4, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors resize-none", placeholder: "Add details about this event" %>
  </div>

  <div class="flex items-center gap-2">
    <%= f.check_box :all_day, class: "h-5 w-5 rounded border-neutral-300 text-primary-600 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20", data: { action: "change->event-form#toggleAllDay" } %>
    <label class="text-sm font-medium text-neutral-700">
      All-day event
    </label>
  </div>

  <div>
    <label class="block text-sm font-medium text-neutral-700 mb-2">
      Starts at
    </label>
    <%= f.datetime_local_field :starts_at, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors", data: { event_form_target: "startTime" } %>
  </div>

  <div>
    <label class="block text-sm font-medium text-neutral-700 mb-2">
      Ends at (optional)
    </label>
    <%= f.datetime_local_field :ends_at, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors", data: { event_form_target: "endTime" } %>
  </div>

  <p class="text-xs text-neutral-500">
    Times are in <%= current_user.couple.timezone %>
  </p>

  <div>
    <label class="block text-sm font-medium text-neutral-700 mb-2">
      Location (optional)
    </label>
    <%= f.text_field :location, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors", placeholder: "Enter location" %>
  </div>

  <div>
    <label class="block text-sm font-medium text-neutral-700 mb-2">
      Category (optional)
    </label>
    <%= f.text_field :category, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors", placeholder: "e.g., Date night, Appointment, Anniversary" %>
  </div>

  <div>
    <label class="block text-sm font-medium text-neutral-700 mb-2">
      Color (optional)
    </label>
    <%= f.color_field :color, class: "w-full h-12 px-2 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors" %>
  </div>

  <div class="flex items-center gap-2">
    <%= f.check_box :requires_response, class: "h-5 w-5 rounded border-neutral-300 text-primary-600 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20" %>
    <label class="text-sm font-medium text-neutral-700">
      Requires RSVP from partner
    </label>
  </div>

  <div class="flex items-center gap-2">
    <%= f.check_box :recurring, checked: event.recurring?, class: "h-5 w-5 rounded border-neutral-300 text-primary-600 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20", data: { action: "change->event-form#toggleRecurrence", event_form_target: "recurringCheckbox" } %>
    <label class="text-sm font-medium text-neutral-700">
      Make this a recurring event
    </label>
  </div>

  <div data-event-form-target="recurrenceFields" class="space-y-4 <%= 'hidden' unless event.recurring? %>">
    <div>
      <label class="block text-sm font-medium text-neutral-700 mb-2">
        Repeat
      </label>
      <%= f.select :recurrence_frequency, options_for_select([['Daily', 'daily'], ['Weekly', 'weekly'], ['Monthly', 'monthly'], ['Yearly', 'yearly']], event.parse_recurrence_rule&.dig(:frequency)), { include_blank: 'Select frequency' }, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors", data: { event_form_target: "frequency" } %>
    </div>

    <div>
      <label class="block text-sm font-medium text-neutral-700 mb-2">
        Every
      </label>
      <%= f.number_field :recurrence_interval, value: event.parse_recurrence_rule&.dig(:interval) || 1, min: 1, max: 30, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors", data: { event_form_target: "interval" } %>
      <p class="text-xs text-neutral-500 mt-1">e.g., 1 for every week, 2 for every other week</p>
    </div>

    <div>
      <label class="block text-sm font-medium text-neutral-700 mb-2">
        Ends on (optional)
      </label>
      <%= f.date_field :recurrence_end_date, value: event.recurrence_end_date, class: "w-full px-4 py-3 rounded-xl border border-neutral-200 focus:border-primary-400 focus:ring-2 focus:ring-primary-400 focus:ring-opacity-20 transition-colors", data: { event_form_target: "endDate" } %>
      <p class="text-xs text-neutral-500 mt-1">Leave blank for no end date</p>
    </div>
  </div>

  <div class="flex justify-end">
    <%= f.submit class: "btn-primary px-6" %>
  </div>
<% end %>

