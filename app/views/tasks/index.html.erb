<div class="bg-gradient-to-br from-white via-primary-50/60 to-purple-50/40 min-h-screen py-12">
  <div class="max-w-6xl mx-auto px-4 space-y-8">
    <!-- Header section -->
    <section class="bg-white rounded-3xl shadow-xl shadow-primary-900/5 border border-neutral-100 p-8">
      <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-6">
        <div>
          <h1 class="text-3xl font-bold text-neutral-900 mb-2">All Tasks</h1>
          <p class="text-neutral-600">Manage your shared to-dos</p>
        </div>
        <div>
          <%= link_to "Add new task", new_task_path, class: "btn-primary justify-center" %>
        </div>
      </div>
    </section>

    <!-- Filter section -->
    <section class="bg-white rounded-3xl shadow-lg shadow-primary-900/5 border border-neutral-100 p-6">
      <h2 class="text-lg font-semibold text-neutral-900 mb-4">Filter tasks</h2>
      <%= form_with url: tasks_path, method: :get, class: "grid grid-cols-1 md:grid-cols-5 gap-4" do |f| %>
        <div>
          <%= f.select :status, [["All statuses", ""]] + Task.statuses.keys.map { |s| [s.humanize, s] }, { selected: params[:status] }, class: "form-control w-full px-4 py-3", name: "status" %>
        </div>
        <div>
          <%= f.collection_select :assignee_id, current_user.couple.users, :id, :name, { include_blank: "All assignees", selected: params[:assignee_id] }, class: "form-control w-full px-4 py-3", name: "assignee_id" %>
        </div>
        <div>
          <%= f.date_field :due_on, class: "form-control w-full px-4 py-3", name: "due_on", placeholder: "Filter by due date" %>
        </div>
        <div>
          <%= f.select :sort, [["Sort by", ""], ["Status (Asc)", "status_asc"], ["Due date (Earliest)", "due_at_asc"], ["Due date (Latest)", "due_at_desc"], ["Priority (High)", "priority_desc"]], { selected: params[:sort] }, class: "form-control w-full px-4 py-3", name: "sort" %>
        </div>
        <div class="md:col-span-1">
          <%= f.submit "Apply filters", class: "btn-secondary w-full md:w-auto md:self-end" %>
        </div>
      <% end %>
      <% if params[:status].present? || params[:assignee_id].present? || params[:due_on].present? || params[:sort].present? %>
        <%= link_to "Clear filters", tasks_path, class: "text-sm text-neutral-600 hover:text-primary-600 mt-4 inline-block" %>
      <% end %>
    </section>

    <!-- Tasks list section -->
    <section>
      <% if @tasks.any? %>
        <h2 class="text-xl font-semibold text-neutral-900 mb-4">Task List</h2>
        <div class="bg-white rounded-2xl border border-neutral-200 p-6">
          <ul class="space-y-3">
            <% @tasks.each do |task| %>
              <%= render "task_item", task: task, compact: false %>
            <% end %>
          </ul>
        </div>
      <% else %>
        <div class="bg-white rounded-2xl border-2 border-dashed border-neutral-200 p-12 text-center">
          <p class="text-neutral-500">No tasks found. Create your first task to get started!</p>
        </div>
      <% end %>
    </section>
  </div>
</div>
