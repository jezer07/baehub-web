<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Baehub" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Baehub">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="Baehub - Connect with your special someone">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= hotwire_native_app? ? 'flex flex-col min-h-screen bg-neutral-50' : 'flex flex-col min-h-screen' %>">
    <% unless hotwire_native_app? %>
    <div data-controller="mobile-menu">
      <div class="fixed inset-0 bg-black/40 backdrop-blur-sm z-40 hidden opacity-0 transition-opacity duration-300" data-mobile-menu-target="backdrop" data-action="click->mobile-menu#close"></div>

      <nav class="sticky top-0 z-50 glass-nav">
        <div class="max-w-6xl mx-auto px-4 sm:px-6">
          <div class="flex justify-between items-center h-16">
            <div class="flex items-center gap-8">
              <a href="/" class="flex items-center gap-2.5 group">
                <span class="text-xl font-bold bg-gradient-to-r from-primary-600 to-primary-500 bg-clip-text text-transparent">Baehub</span>
              </a>
              <% if user_signed_in? %>
                <div class="hidden md:flex items-center gap-1">
                  <%= link_to "Tasks", tasks_path, class: "text-neutral-600 hover:text-neutral-900 hover:bg-neutral-100 px-3 py-2 rounded-lg text-sm font-medium transition-all" %>
                  <%= link_to "Events", events_path, class: "text-neutral-600 hover:text-neutral-900 hover:bg-neutral-100 px-3 py-2 rounded-lg text-sm font-medium transition-all" %>
                  <%= link_to "Expenses", expenses_path, class: "text-neutral-600 hover:text-neutral-900 hover:bg-neutral-100 px-3 py-2 rounded-lg text-sm font-medium transition-all" %>
                  <%= link_to "Settings", settings_path, class: "text-neutral-600 hover:text-neutral-900 hover:bg-neutral-100 px-3 py-2 rounded-lg text-sm font-medium transition-all" %>
                </div>
              <% end %>
            </div>

            <div class="hidden md:flex items-center gap-3">
              <% if user_signed_in? %>
                <div class="flex items-center gap-3">
                  <span class="text-sm text-neutral-500"><%= current_user.name %></span>
                  <div class="h-5 w-px bg-neutral-200"></div>
                  <%= link_to "Account", edit_user_registration_path, class: "text-sm text-neutral-500 hover:text-neutral-700 transition-colors" %>
                  <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-sm text-neutral-500 hover:text-neutral-700 transition-colors" %>
                </div>
              <% else %>
                <%= link_to "Sign In", new_user_session_path, class: "text-sm text-neutral-600 hover:text-neutral-900 px-3 py-2 rounded-lg font-medium transition-all" %>
                <%= link_to "Sign Up", new_user_registration_path, class: "btn-primary" %>
              <% end %>
            </div>

            <div class="md:hidden">
              <button type="button" class="inline-flex items-center justify-center p-2 rounded-lg text-neutral-500 hover:text-neutral-700 hover:bg-neutral-100 focus:outline-none transition-all" data-action="click->mobile-menu#toggle">
                <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                </svg>
              </button>
            </div>
          </div>
        </div>

        <div class="fixed top-0 right-0 h-full w-72 bg-white shadow-elevated z-50 transform translate-x-full transition-transform duration-300 hidden md:hidden border-l border-neutral-200/50" data-mobile-menu-target="menu">
          <div class="p-5 border-b border-neutral-100 flex justify-between items-center">
            <span class="text-base font-semibold text-neutral-900">Menu</span>
            <button type="button" class="p-1.5 rounded-lg text-neutral-400 hover:text-neutral-600 hover:bg-neutral-100 transition-all" data-action="click->mobile-menu#close">
              <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </div>
          <div class="p-3 space-y-1">
            <% if user_signed_in? %>
              <div class="px-3 py-2.5 mb-2">
                <p class="text-sm font-medium text-neutral-900"><%= current_user.name %></p>
                <p class="text-xs text-neutral-500 mt-0.5"><%= current_user.email %></p>
              </div>
              <div class="h-px bg-neutral-100 my-2"></div>
              <%= link_to "Tasks", tasks_path, class: "flex items-center gap-3 text-neutral-600 hover:text-neutral-900 hover:bg-neutral-50 px-3 py-2.5 rounded-xl text-sm font-medium transition-all", data: { action: "click->mobile-menu#close" } %>
              <%= link_to "Events", events_path, class: "flex items-center gap-3 text-neutral-600 hover:text-neutral-900 hover:bg-neutral-50 px-3 py-2.5 rounded-xl text-sm font-medium transition-all", data: { action: "click->mobile-menu#close" } %>
              <%= link_to "Expenses", expenses_path, class: "flex items-center gap-3 text-neutral-600 hover:text-neutral-900 hover:bg-neutral-50 px-3 py-2.5 rounded-xl text-sm font-medium transition-all", data: { action: "click->mobile-menu#close" } %>
              <%= link_to "Settings", settings_path, class: "flex items-center gap-3 text-neutral-600 hover:text-neutral-900 hover:bg-neutral-50 px-3 py-2.5 rounded-xl text-sm font-medium transition-all", data: { action: "click->mobile-menu#close" } %>
              <div class="h-px bg-neutral-100 my-2"></div>
              <%= link_to "Account", edit_user_registration_path, class: "flex items-center gap-3 text-neutral-500 hover:text-neutral-700 hover:bg-neutral-50 px-3 py-2.5 rounded-xl text-sm transition-all", data: { action: "click->mobile-menu#close" } %>
              <div class="px-3 pt-2">
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "w-full btn-secondary justify-center text-sm", data: { action: "click->mobile-menu#close" } %>
              </div>
            <% else %>
              <%= link_to "Sign In", new_user_session_path, class: "flex items-center gap-3 text-neutral-600 hover:text-neutral-900 hover:bg-neutral-50 px-3 py-2.5 rounded-xl text-sm font-medium transition-all", data: { action: "click->mobile-menu#close" } %>
              <div class="px-3 pt-2">
                <%= link_to "Sign Up", new_user_registration_path, class: "w-full btn-primary justify-center", data: { action: "click->mobile-menu#close" } %>
              </div>
            <% end %>
          </div>
        </div>
      </nav>
    </div>
    <% end %>

    <div id="flash-messages"></div>

    <% if flash.any? %>
      <div class="<%= hotwire_native_app? ? 'px-4 mt-3' : 'max-w-6xl mx-auto px-4 sm:px-6 mt-4' %>">
        <% flash.each do |type, message| %>
          <div class="<%= flash_class(type) %> border rounded-xl p-4 mb-3 flex items-start animate-fade-in" data-controller="flash">
            <div class="flex-shrink-0">
              <%= flash_icon(type) %>
            </div>
            <div class="ml-3 flex-1">
              <p class="text-sm font-medium"><%= message %></p>
            </div>
            <div class="ml-auto pl-3">
              <button type="button" class="inline-flex rounded-lg p-1 hover:bg-black/5 focus:outline-none transition-colors" data-action="click->flash#dismiss">
                <svg class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd" />
                </svg>
              </button>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="<%= hotwire_native_app? ? 'flex-1 w-full px-4 py-4' : 'flex-1 max-w-6xl w-full mx-auto px-4 sm:px-6 py-8' %>">
      <%= yield %>
    </main>

    <% unless hotwire_native_app? %>
    <footer class="hidden md:block border-t border-neutral-100 mt-auto">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 py-8">
        <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
          <div class="flex items-center gap-6">
            <span class="text-sm font-semibold bg-gradient-to-r from-primary-600 to-primary-500 bg-clip-text text-transparent">Baehub</span>
            <span class="text-xs text-neutral-400">&copy; <%= Date.current.year %> All rights reserved.</span>
          </div>
          <div class="flex items-center gap-6">
            <a href="#" class="text-xs text-neutral-400 hover:text-neutral-600 transition-colors">About</a>
            <a href="#" class="text-xs text-neutral-400 hover:text-neutral-600 transition-colors">Privacy</a>
            <a href="#" class="text-xs text-neutral-400 hover:text-neutral-600 transition-colors">Terms</a>
          </div>
        </div>
      </div>
    </footer>
    <% end %>
  </body>
</html>
