<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #10B981; color: white; padding: 20px; text-align: center; }
      .content { background-color: #f9fafb; padding: 30px; }
      .settlement-details { background-color: white; padding: 20px; border-radius: 8px; margin: 20px 0; }
      .amount { font-size: 24px; font-weight: bold; color: #10B981; }
      .button { display: inline-block; padding: 12px 24px; background-color: #10B981; color: white; text-decoration: none; border-radius: 6px; }
      .footer { text-align: center; padding: 20px; color: #6b7280; font-size: 12px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Settlement Completed</h1>
      </div>
      
      <div class="content">
        <p>Hi <%= @recipient.name %>,</p>
        
        <p>A settlement has been completed on <strong><%= @joint_account.name %></strong>.</p>
        
        <div class="settlement-details">
          <p><strong>Settled by:</strong> <%= @settled_by.name %></p>
          <p><strong>Amount:</strong> <span class="amount"><%= @settlement.currency_symbol %><%= sprintf('%.2f', @settlement.total_amount_cents / 100.0) %></span></p>
          <% if @settlement.notes.present? %>
            <p><strong>Notes:</strong> <%= @settlement.notes %></p>
          <% end %>
          <% if @settlement.payment_method.present? %>
            <p><strong>Payment method:</strong> <%= @settlement.payment_method.titleize %></p>
          <% end %>
          <p><strong>Date:</strong> <%= @settlement.settlement_date.strftime("%B %d, %Y") %></p>
          <p><strong>Entries settled:</strong> <%= @settlement.ledger_entries_count %></p>
        </div>
        
        <p style="text-align: center; margin: 30px 0;">
          <a href="<%= joint_account_url(@joint_account) %>" class="button">View Joint Account</a>
        </p>
      </div>
      
      <div class="footer">
        <p>This is an automated message from Baehub</p>
      </div>
    </div>
  </body>
</html>

